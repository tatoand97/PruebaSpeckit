# Spec Kit History

**LEGACY — UNSUPPORTED — NOT USED BY THE CURRENT WORKFLOW**

This directory preserves user-authored and generated Spec Kit evidence for traceability. Its files are read-only historical sources. The active SDD system is the single repository-root `openspec/` tree.

- `constitution.md` is the unmodified historical governance source recovered from commit `c3b3349`.
- `001-registrar-contacto/` contains the complete implemented feature artifacts recovered from the same commit, including specs, plan, research, tasks, checklists, OpenAPI, quickstart, data model, and implementation evidence.
