## 1. <Dependency-First Workstream>

- [ ] 1.1 `<repository/path>` — <specific executable change>; test with `<test path or command>`; complete when <observable criterion>.

## 2. Verification

- [ ] 2.1 Run `<exact verification command>` and retain <evidence path or result>.
- [ ] 2.2 Run `./scripts/Invoke-OpenSpecSddGuard.ps1`; complete only when all mandatory checks pass.

<!--
Rules:
- Order tasks by dependency and use sequential X.Y identifiers.
- Every task names concrete paths, associated tests, a command, and observable completion evidence.
- Do not use generic wording such as "implement service" or "add tests".
-->
